{
  "name": "atcs-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "decode": "yarn --cwd packages/decoder start"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
